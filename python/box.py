#!/usr/bin/env python

# get user input
context = raw_input('Please input your context:')

# get user's context
context_len = len(context)

print('\n')
print('+' + '-' * context_len + '+')
print('|' + context + "|")
print('+' + '-' * context_len + '+')

